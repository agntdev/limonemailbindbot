[
  {
    "name": "/list_bindings",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Admin-only: list current bindings (works only in ADMIN_CHAT_ID)"
            }
          }
        ],
        "send": {
          "text": "/list_bindings"
        }
      }
    ]
  }
]
