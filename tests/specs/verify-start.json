[
  {
    "name": "Enter code",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Prompt to enter the verification code (ForceReply)"
            }
          }
        ],
        "send": {
          "callback": "verify:start"
        }
      }
    ]
  }
]
